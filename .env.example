# AI artwork generation requires a server-side OpenAI API key.
OPENAI_API_KEY=
OPENAI_IMAGE_MODEL=gpt-image-2.5-sunburst

# Optional shared-catalog overrides. The app has safe publishable defaults.
FLYERS_CATALOG_URL=
FLYERS_CATALOG_KEY=
